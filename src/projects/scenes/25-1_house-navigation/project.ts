import { makeProject } from "@motion-canvas/core";
import scene from "./scene?scene";

export default makeProject({
  name: "25-1_house-navigation",
  scenes: [scene],
  experimentalFeatures: true,
});
